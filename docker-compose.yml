# Desarrollo local con Docker. Uso: docker compose up -d
# Frontend (build con VITE_API_BASE=http://localhost:3000) y Backend hablan por puertos del host.

services:
  postgres:
<<<<<<< HEAD
    image: postgres:16
=======
    image: postgres:16-alpine
>>>>>>> 00efccea6476f5df81d3370e2033824234b5dd82
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: academic
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: .
      dockerfile: packages/backend/Dockerfile
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
      JWT_SECRET: change-me-in-production
      TYPEORM_HOST: postgres
      TYPEORM_PORT: 5432
      TYPEORM_USERNAME: postgres
      TYPEORM_PASSWORD: postgres
      TYPEORM_DATABASE: academic
      STUDENT_DEFAULT_PASSWORD: TempStudent1!
    depends_on:
      postgres:
        condition: service_healthy

  frontend:
    build:
      context: .
      dockerfile: packages/frontend/Dockerfile
      args:
        VITE_API_BASE: http://localhost:3000
    ports:
      - "8080:80"
    depends_on:
      - backend
